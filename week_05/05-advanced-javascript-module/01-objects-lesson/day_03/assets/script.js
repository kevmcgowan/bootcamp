// var results = data.results

// // for (var items of results) {
// //     if (items.pilots.length > 0) {
// //         console.log(items.pilots);
// //     } 
// // }


// for (var resultsIndex = 0; resultsIndex < results.length; resultsIndex++) {
//     var starshipData = results[resultsIndex];
//     var pilots = starshipData.pilots;
    
//     if (pilots.length > 0) {
//     for (var pilotURL of pilots) {
//         console.log(`${starshipData.name} - ${pilotURL}`)
//     }
//     }
// }

// for (var i = 0; i < data.results.length; i++) {
//     if (data.results[i].pilots.length > 0);{
//     console.log(data.results[i].pilots);
//     }
// }

// for - keyword to say we are starting a for loop
//var i=0 ; the variable that we are starting our loop at
// condiiton( our example: i < data.results.length); If this condition is true, continue to run the executable code
//i++ ; Once we finish running the executable coed, we are going to increase our looping looping variable by one. Then go back to condition
// Once the condition is false, then it exits the code
  
  
  
 